{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "talisma.fullname" . }}
  labels:
    {{- include "talisma.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.global.domain | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "talisma.fullname" $ }}-frontend
                port:
                  name: http
          {{- if $.Values.mcpServer.enabled }}
          - path: /mcp
            pathType: Prefix
            backend:
              service:
                name: {{ include "talisma.fullname" $ }}-mcp-server
                port:
                  name: http
          {{- end }}
{{- end }}
