{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "talisma.fullname" . }}-postgresql
  labels:
    {{- include "talisma.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: tcp-postgresql
      protocol: TCP
      name: tcp-postgresql
  selector:
    {{- include "talisma.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
{{- end }}
